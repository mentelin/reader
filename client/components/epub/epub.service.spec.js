'use strict';

describe('Service: Epub', function () {

  // load the service's module
  beforeEach(module('readerApp'));

  // instantiate service
  var epub;
  beforeEach(inject(function (_epub_) {
    epub = _epub_;
  }));

  // it('should do something', function () {
  //   expect(!!epub).toBe(true);
  // });

});
